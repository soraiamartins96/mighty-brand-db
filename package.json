{
  "name": "mighty-brand-db",
  "version": "1.0.0",
  "description": "Mighty Brand DB — serves the brand library UI from Postgres + object storage.",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
